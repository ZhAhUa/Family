<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finance.familyfinance"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="20"
        android:targetSdkVersion="20" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 注册 IndexActivity  -->
        <activity 
             android:name=".IndexActivity"
            android:label="@string/app_name" >            
        </activity>
        <!-- 注册 HomeActivity  -->
         <activity
            android:name=".HomeActivity"
            android:label="@string/app_name" >
        </activity>
        <!-- 注册 MessageActivity  -->
         <activity
            android:name=".MessageActivity"
            android:label="@string/app_name" >
        </activity>
           <!-- 注册 FlagActivity  -->
         <activity
            android:name=".FlagActivity"
            android:label="@string/app_name" >
        </activity>
           <!-- 注册 SelfActivity  -->
         <activity
            android:name=".SelfActivity"
            android:label="@string/app_name" >
        </activity>
           <!-- 注册 TotalData  -->
         <activity
            android:name=".TotalData"
            android:label="@string/app_name" >
        </activity>
          <!-- 注册 Total_InList  -->
         <activity
            android:name=".Total_InList"
            android:label="@string/app_name" >
        </activity>
          <!-- 注册 Total_OutList  -->
         <activity
            android:name=".Total_OutList"
            android:label="@string/app_name" >
        </activity>
          <!-- 注册 OutCostEdit  -->
         <activity
            android:name=".OutCostEdit"
            android:label="@string/app_name" >
        </activity>
          <!-- 注册 OutCostList  -->
         <activity
            android:name=".OutCostList"
            android:label="@string/app_name" >
        </activity>
          <!-- 注册 InCostEdit  -->
         <activity
            android:name=".InCostEdit"
            android:label="@string/app_name" >
        </activity>
         <!-- 注册 InCostList  -->
         <activity
            android:name=".InCostList"
            android:label="@string/app_name" >
        </activity>
         <!-- 注册 FlagEdit  -->
         <activity
            android:name=".FlagEdit"
            android:label="@string/app_name" >
        </activity>
         <!-- 注册 FlagList  -->
         <activity
            android:name=".FlagList"
            android:label="@string/app_name" >
        </activity>
        <!-- 注册 FlagList  -->
         <activity
            android:name=".UserSetInfo"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>
